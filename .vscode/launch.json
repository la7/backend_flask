{
    "version": "0.2.0",
    "configurations": [
        
        {
            "name": "Run Flask (backend_flask)",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/app/app.py",
            "console": "integratedTerminal",
            "env": {
                "FLASK_ENV": "development"
            },
            "justMyCode": true
        }
    ]
}